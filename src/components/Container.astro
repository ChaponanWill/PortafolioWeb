---
import * as CONTENT from "../pages/content"
import AccordionLayout from "../layouts/AccordionLayout.astro";
import Card from "./Card.astro";
---

<ul class="shadow-box">
  <AccordionLayout title={"Acerca de mi"} icon={"mdi:account-badge-outline"}>
    {CONTENT.about}
  </AccordionLayout>
    <AccordionLayout title={"Trabajos"} icon={"ic:baseline-work-outline"}>
      {
        CONTENT.work.map((item) => {
          return (
            <Card
              title={item.role}
              timeframe={`${item.from} - ${item.to}`}
              description={item.location}
              tags={item.skills}
              url={item.org_url}
              url_name={item.org}
            />
          );
        })
      }
    </AccordionLayout>

    <AccordionLayout title={"Proyectos"} icon={"ion:md-build"}>
      {
        CONTENT.projects.map((item) => {
          return (
            <Card
              title={item.title}
              timeframe={`${item.from} - ${item.to}`}
              description={item.description}
              tags={item.tech}
              url={item.url}
              url_name={"Abrir proyecto"}
            />
          );
        })
      }
    </AccordionLayout>

    <AccordionLayout title={"Educación"} icon={"carbon:education"}>
      {
        CONTENT.education.map((item) => {
          return (
            <Card
              title={item.course}
              timeframe={`${item.from} - ${item.to}`}
              description={item.location}
              tags={""}
              url={"#"}
              url_name={item.institute}
            />
          );
        })
      }
    </AccordionLayout>

    <AccordionLayout title={"Certificaciones"} icon={"carbon:certificate"}>
      {
        CONTENT.certificates.map((item) => {
          return (
            <Card
              title={item.title}
              timeframe={item.issued}
              description={item.org}
              tags={item.description}
              url={item.url}
              url_name={"Ver certificado"}
            />
          );
        })
      }
    </AccordionLayout>

    <AccordionLayout title={"Blogs"} icon={"ic:round-edit-note"}>
      {
        CONTENT.blogs.map((item) => {
          return (
            <Card
              title={item.title}
              timeframe={item.date}
              description={item.description}
              tags=""
              url={item.url}
              url_name={item.publisher}
            />
          );
        })
      }
    </AccordionLayout>
    <!-- --- -->


    <AccordionLayout title={"Contacto"} icon={"ri:contacts-book-fill"}>
      {
        CONTENT.contact.map((item) => {
          return (
            <Card
              title={""}
              timeframe=""
              description=""
              tags=""
              url={item.source}
              url_name={item.source_name}
            />
          );
        })
      }
    </AccordionLayout>
</ul>
